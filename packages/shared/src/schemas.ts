// Shared schemas removed — validation uses class-validator DTOs in the API.
// If Zod validation is needed in the future, add schemas here and wire them
// with a ZodValidationPipe in the API layer.
