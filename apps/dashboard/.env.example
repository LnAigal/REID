NEXT_PUBLIC_API_URL=http://localhost:3001/api/v1
NEXT_PUBLIC_APP_NAME=REID
NEXT_PUBLIC_DOCS_URL=http://localhost:3002

# Must match apps/api JWT_SECRET — the dashboard middleware uses it to
# verify auth cookies on protected routes.
JWT_SECRET=change-me-to-match-the-api-jwt-secret
